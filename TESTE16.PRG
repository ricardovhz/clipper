#INCLUDE "INKEY.CH"
SETCOLOR("W+/B,N/W,B,N/W")
CLS

WHILE .T.
   LEI = SPACE(63)
   PFFUNC = "@K!S16"
   @ 05,10 GET LEI PICTURE PFFUNC
   READ
   IF LASTKEY()==K_ESC
      EXIT
   ENDIF
END
SETCOLOR("W/N,W/N,N,W/N")
CLS
RETURN NIL
