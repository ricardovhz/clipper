#INCLUDE "INKEY.CH"

WHILE .T.
   CLS
   CPF5 := SPACE(11)
   @ 13,35 SAY "Digite o seu CPF " GET CPF5 PICTURE "@R 999.999.999-99"
   READ
   IF LASTKEY()==K_ESC
      EXIT
   ENDIF
   INKEY(0)
END
SETCOLOR("W/N,W/N,,,W/N")
CLS
RETURN NIL
